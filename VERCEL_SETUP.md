# Инструкция по настройке Vercel

## 🔴 ВАЖНО: Настройка переменной окружения

Если вы видите ошибку `OPENAI_API_KEY not configured`, выполните следующие шаги:

### Шаг 1: Получите ваш OpenAI API ключ

1. Найдите ваш `.env` файл в корне проекта
2. Откройте его и скопируйте значение переменной `VITE_OPENAI_API_KEY` (без кавычек)

### Шаг 2: Добавьте переменную в Vercel

1. Зайдите на https://vercel.com и авторизуйтесь
2. Выберите ваш проект **owleys-scenario-builder**
3. Перейдите в **Settings** (настройки)
4. В левом меню выберите **Environment Variables**
5. Нажмите **Add New** (или **Add**)
6. Заполните форму:
   - **Key** (Имя): `OPENAI_API_KEY`
   - **Value** (Значение): вставьте ваш API ключ из `.env` файла
   - **Environment**: выберите все три:
     - ✅ Production
     - ✅ Preview  
     - ✅ Development
7. Нажмите **Save**

### Шаг 3: Перезапустите деплой

**ВАЖНО**: После добавления переменной окружения нужно перезапустить деплой!

1. В Vercel Dashboard перейдите в **Deployments**
2. Найдите последний деплой (вверху списка)
3. Нажмите на три точки (⋯) справа от деплоя
4. Выберите **Redeploy**
5. Подтвердите перезапуск

ИЛИ:

- Сделайте любой небольшой коммит в Git (например, добавьте пробел в README.md)
- Vercel автоматически запустит новый деплой с новыми переменными окружения

### Шаг 4: Проверьте логи

1. После перезапуска зайдите в **Deployments** → выберите новый деплой
2. Перейдите в **Functions** → `/api/scenarios`
3. Проверьте **Logs** — не должно быть ошибок про `OPENAI_API_KEY`

---

## 🔧 Дополнительные настройки

### Настройка проекта

- Vercel автоматически обнаружит `api/` папку с serverless функциями
- Функция `/api/scenarios` будет доступна по адресу: `https://ваш-домен.vercel.app/api/scenarios`

### Локальная разработка

Для локальной разработки с serverless функциями:
```bash
npm install -g vercel
vercel dev
```

Это запустит локальный сервер с поддержкой serverless функций и переменных окружения.

### Проверка переменных окружения

Если после добавления переменной ошибка все еще есть:

1. Убедитесь, что переменная называется именно `OPENAI_API_KEY` (без `VITE_` префикса!)
2. Убедитесь, что переменная добавлена для всех окружений (Production, Preview, Development)
3. Проверьте, что значение скопировано полностью (без лишних пробелов)
4. Обязательно перезапустите деплой после добавления переменной

---

## ⚠️ Важно

- API ключ OpenAI **должен быть** добавлен в Vercel Environment Variables
- Название переменной должно быть **`OPENAI_API_KEY`** (не `VITE_OPENAI_API_KEY`!)
- Функция имеет таймаут 60 секунд (настроено в `vercel.json`)
- В production используется модель `gpt-4o` (можно изменить в `api/scenarios.js`)

---

## 🐛 Решение проблем

**Ошибка: "OPENAI_API_KEY not configured"**
- ✅ Проверьте, что переменная добавлена в Vercel
- ✅ Убедитесь, что название переменной именно `OPENAI_API_KEY`
- ✅ Перезапустите деплой после добавления переменной

**Ошибка: "Failed to fetch"**
- Проверьте, что функция деплоится успешно
- Проверьте логи функции в Vercel Dashboard
- Убедитесь, что CORS настроен правильно

**Долгая загрузка**
- Функция может выполняться до 60 секунд (таймаут настроен в `vercel.json`)
- Это нормально для генерации сценариев через OpenAI API
