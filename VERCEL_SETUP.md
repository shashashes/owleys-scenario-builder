# Инструкция по настройке Vercel

## 🔧 Необходимые шаги:

### 1. Добавьте переменную окружения в Vercel

1. Зайдите в ваш проект на Vercel Dashboard
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте переменную:
   - **Name**: `OPENAI_API_KEY`
   - **Value**: ваш OpenAI API ключ (из `.env` файла, переменная `VITE_OPENAI_API_KEY`)
   - **Environment**: выберите **Production**, **Preview** и **Development**

### 2. Убедитесь, что проект деплоится

- Vercel автоматически обнаружит `api/` папку с serverless функциями
- Функция `/api/scenarios` будет доступна по адресу: `https://ваш-домен.vercel.app/api/scenarios`

### 3. Проверьте логи

Если есть ошибки:
- Зайдите в **Deployments** → выберите последний деплой → **Logs**
- Или **Functions** → `/api/scenarios` → **Logs**

### 4. Локальная разработка

Для локальной разработки с serverless функциями:
```bash
npm install -g vercel
vercel dev
```

Это запустит локальный сервер с поддержкой serverless функций.

## ⚠️ Важно

- API ключ OpenAI **должен быть** добавлен в Vercel Environment Variables
- Функция имеет таймаут 60 секунд (настроено в `vercel.json`)
- В production используется модель `gpt-4o` (можно изменить в `api/scenarios.js`)

