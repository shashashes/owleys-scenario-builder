# 📸 Инструкция по добавлению изображений

## Быстрый старт

### Шаг 1: Поместите изображения в папку
Скопируйте все изображения товаров в папку:
```
public/images/
```

### Шаг 2: Именование файлов
Назовите файлы по **Item ID** или **SKU** из CSV:

**Вариант A: По Item ID**
- `p-3014-10.jpg` → для товара с Item ID = `p-3014-10`
- `p-3014-9.png` → для товара с Item ID = `p-3014-9`

**Вариант B: По SKU**
- `OUTR01-01A.jpg` → для товара с SKU = `OUTR01-01A  (old)`
- `OUSC01-01A.png` → для товара с SKU = `OUSC01-01A`

**Поддерживаемые форматы:** `.jpg`, `.jpeg`, `.png`, `.webp`, `.gif`, `.svg`

### Шаг 3: Автоматическое добавление путей в CSV

Запустите скрипт:
```bash
node scripts/add-images.js
```

Скрипт автоматически:
- Найдет все изображения в `public/images/`
- Сопоставит их с товарами по Item ID или SKU
- Добавит пути к изображениям в колонку "BOX Picture" в CSV

### Шаг 4: Проверка
Перезапустите dev сервер (если он запущен):
```bash
npm run dev
```

Изображения должны появиться в интерфейсе!

---

## Ручное добавление (альтернатива)

Если хотите добавить пути вручную, откройте `public/data/items.csv` и в колонке **"BOX Picture"** укажите:

**Для локальных файлов:**
```
item-001.jpg
```
или
```
./images/item-001.jpg
```

**Для URL:**
```
https://example.com/images/product.jpg
```

---

## Примеры именования

| Item ID | SKU | Имя файла изображения |
|---------|-----|----------------------|
| p-3014-10 | OUTR01-01A | `p-3014-10.jpg` или `OUTR01-01A.jpg` |
| p-3014-9 | OUTR01-01B | `p-3014-9.png` или `OUTR01-01B.png` |
| p-3014-17 | OUSC01-01A | `p-3014-17.jpg` или `OUSC01-01A.jpg` |

---

## Структура папок

```
owleys-scenario-builder/
├── public/
│   ├── images/          ← Поместите изображения сюда
│   │   ├── p-3014-10.jpg
│   │   ├── p-3014-9.png
│   │   └── ...
│   └── data/
│       └── items.csv    ← Пути добавятся сюда автоматически
└── scripts/
    └── add-images.js    ← Скрипт для автоматизации
```

---

## Устранение проблем

**Изображения не отображаются?**
1. Проверьте, что файлы находятся в `public/images/`
2. Проверьте имена файлов (должны совпадать с Item ID или SKU)
3. Проверьте формат файла (jpg, png, webp и т.д.)
4. Откройте консоль браузера (F12) и проверьте ошибки загрузки

**Скрипт не находит изображения?**
- Убедитесь, что имена файлов точно совпадают с Item ID или SKU (без пробелов, регистр не важен)
- Проверьте, что файлы имеют правильное расширение

---

## Массовое добавление изображений

Если у вас много изображений и они названы по-другому, можно:

1. Переименовать файлы по Item ID или SKU
2. Или вручную обновить CSV, добавив пути в колонку "BOX Picture"

